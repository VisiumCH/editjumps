This directory holds a vendored MMseqs2 static binary reference/license.
The binary itself lives at .venv/bin/mmseqs (installed by editjumps/core/install_mmseqs.sh,
outside the .venv's uv-managed package set since MMseqs2 is a compiled tool, not
a Python package). Source: https://mmseqs.com/latest (universal macOS binary).

